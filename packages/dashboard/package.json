{
  "name": "@tah/dashboard",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "build": "tsc -b && node -e \"const fs=require('fs');const path=require('path');fs.cpSync(path.resolve('src/public'),path.resolve('dist/public'),{recursive:true});\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@tah/orchestrator": "*",
    "fastify": "^5.12.1"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
