{
  "name": "@tah/orchestrator",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@tah/proxy": "*",
    "@tah/verdict": "*",
    "ajv": "^8.12.0",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}